_format_version: "3.0"

services:
  - name: kata-mongodb
    url: http://app:8080
    plugins:
      - name: pre-function
        config:
          access:
            - |
              __AUTHORIZER_LUA__
    routes:
      - name: kata-mongodb-route
        paths:
          - /
        strip_path: false
